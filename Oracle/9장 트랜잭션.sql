/*
250812
최민수
트랜잭션
*/

-- 실습 8-1
SELECT * FROM ACCOUNT;

UPDATE ACCOUNT SET
    ACC_BALANCE = ACC_BALANCE - 10000
    WHERE ACC_NO = '101-11-1001';
    
UPDATE ACCOUNT SET
    ACC_BALANCE = ACC_BALANCE + 10000
    WHERE ACC_NO = '101-11-1003';
    
COMMIT;

-- 롤백
SELECT * FROM ACCOUNT;

UPDATE ACCOUNT SET
    ACC_BALANCE = ACC_BALANCE - 10000
    WHERE ACC_NO = '101-11-1001';
    
UPDATE ACCOUNT SET
    ACC_BALANCE = ACC_BALANCE + 10000
    WHERE ACC_NO = '101-11-1003';
    
ROLLBACK;


-- 실습 8-3
SHOW AUTOCOMMIT;

SET AUTOCOMMIT OFF;

SELECT * FROM ACCOUNT;

UPDATE ACCOUNT SET
    ACC_BALANCE = ACC_BALANCE - 10000
    WHERE ACC_NO = '101-11-1001';
    
UPDATE ACCOUNT SET
    ACC_BALANCE = ACC_BALANCE + 10000
    WHERE ACC_NO = '101-11-1003';









